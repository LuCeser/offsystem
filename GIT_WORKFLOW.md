# Git 工作流程指南

这个文档描述了如何使用Git来管理你的个人博客项目。

## 📋 基本命令

### 查看状态
```bash
git status                    # 查看当前仓库状态
git log --oneline --graph    # 查看提交历史
```

### 添加和提交
```bash
git add .                    # 添加所有更改
git add 文件名               # 添加特定文件
git commit -m "提交信息"     # 提交更改
```

### 分支管理
```bash
git branch                   # 查看所有分支
git branch 新分支名         # 创建新分支
git checkout 分支名         # 切换分支
git merge 分支名            # 合并分支
```

## 🔄 日常开发流程

### 1. 开始新功能开发
```bash
git checkout -b feature/新功能名称
# 进行开发...
git add .
git commit -m "feat: 添加新功能描述"
```

### 2. 修复问题
```bash
git checkout -b fix/问题描述
# 进行修复...
git add .
git commit -m "fix: 修复问题描述"
```

### 3. 更新文章
```bash
git add articles/新文章.md
git commit -m "docs: 添加新文章《文章标题》"
```

## 📝 提交信息规范

使用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

- `feat:` 新功能
- `fix:` 修复bug
- `docs:` 文档更新
- `style:` 代码格式调整
- `refactor:` 代码重构
- `chore:` 构建过程或辅助工具的变动

### 示例
```bash
git commit -m "feat: 添加文章搜索功能"
git commit -m "fix: 修复移动端导航菜单问题"
git commit -m "docs: 更新README文档"
git commit -m "style: 调整文章卡片间距"
```

## 🚀 推送到远程仓库

如果将来需要推送到GitHub等远程仓库：

```bash
git remote add origin <仓库URL>
git push -u origin master
```

## 📦 备份和恢复

### 创建备份
```bash
git tag -a v1.0 -m "版本1.0发布"
git tag                    # 查看所有标签
```

### 恢复到特定版本
```bash
git log --oneline          # 查看提交历史
git checkout 提交hash     # 恢复到特定提交
```

## 🔧 有用的配置

### 配置用户信息
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

### 配置默认分支名
```bash
git config --global init.defaultBranch main
```

### 配置编辑器
```bash
git config --global core.editor "nano"  # 或 vim, code 等
```

## 📱 常见问题解决

### 撤销最后的提交
```bash
git reset --soft HEAD~1   # 保留更改
git reset --hard HEAD~1   # 删除更改
```

### 修改最后一次提交信息
```bash
git commit --amend -m "新的提交信息"
```

### 忽略已跟踪的文件
```bash
echo "文件名" >> .gitignore
git rm --cached 文件名
git add .gitignore
git commit -m "chore: 更新gitignore"
```

## 🎯 建议的分支策略

对于个人博客项目，可以使用简单的分支策略：

- `master` 主分支：稳定的线上版本
- `develop` 开发分支：日常开发
- `feature/*` 功能分支：新功能开发
- `fix/*` 修复分支：bug修复

### 工作流程
1. 从 `develop` 创建功能分支
2. 在功能分支上开发
3. 完成后合并回 `develop`
4. 定期将 `develop` 合并到 `master` 发布

这样可以保持主分支的稳定性，同时允许自由的开发和实验。